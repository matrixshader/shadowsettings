---
phase: 01-app-identity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meson.build
  - src/main.vala
  - src/application.vala
  - src/window.vala
  - src/panels/power.vala
  - src/panels/windows.vala
  - src/panels/desktop.vala
  - src/panels/appearance.vala
  - src/panels/input.vala
  - src/helpers/logind-helper.vala
  - data/io.github.matrixshader.ShadowSettings.desktop
  - polkit/io.github.matrixshader.ShadowSettings.policy
  - data/icons/hicolor/scalable/apps/io.github.matrixshader.ShadowSettings.svg
autonomous: true
requirements:
  - NFR-6

must_haves:
  truths:
    - "App ID is io.github.matrixshader.ShadowSettings everywhere"
    - "Binary is named shadow-settings, not construct"
    - "No file or string reference to 'construct' or 'com.github.matrixshader.construct' remains"
    - "Window title says Shadow Settings, not The Construct"
    - "App builds clean and launches"
  artifacts:
    - path: "meson.build"
      provides: "Build config with new project name, binary name, and install paths"
      contains: "shadow-settings"
    - path: "src/application.vala"
      provides: "Application class with new app ID and namespace"
      contains: "io.github.matrixshader.ShadowSettings"
    - path: "data/io.github.matrixshader.ShadowSettings.desktop"
      provides: "Desktop entry with new name, icon, and exec"
      contains: "Exec=shadow-settings"
    - path: "polkit/io.github.matrixshader.ShadowSettings.policy"
      provides: "Polkit policy with new action ID"
      contains: "io.github.matrixshader.ShadowSettings.write-system-config"
  key_links:
    - from: "meson.build"
      to: "data/io.github.matrixshader.ShadowSettings.desktop"
      via: "install_data path"
      pattern: "io\\.github\\.matrixshader\\.ShadowSettings\\.desktop"
    - from: "meson.build"
      to: "polkit/io.github.matrixshader.ShadowSettings.policy"
      via: "install_data path"
      pattern: "io\\.github\\.matrixshader\\.ShadowSettings\\.policy"
    - from: "src/application.vala"
      to: "data/io.github.matrixshader.ShadowSettings.desktop"
      via: "application_id matches desktop file name"
      pattern: "io\\.github\\.matrixshader\\.ShadowSettings"
---

<objective>
Rename the app from "Construct" to "Shadow Settings" and set the permanent app ID to `io.github.matrixshader.ShadowSettings`.

Purpose: The app ID is permanent for Flathub -- every file (desktop, polkit, icons, meson.build) keys off it. Flathub requires `io.github.*` not `com.github.*`. All branding must change before any other work.

Output: All source files, data files, and build config updated with new identity. App builds and launches as `shadow-settings`.
</objective>

<execution_context>
@/home/neo/.claude/get-shit-done/workflows/execute-plan.md
@/home/neo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md (Pitfall 5: App ID, Pitfall 15: polkit mismatch)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename data files and update their contents</name>
  <files>
    data/io.github.matrixshader.ShadowSettings.desktop,
    polkit/io.github.matrixshader.ShadowSettings.policy,
    data/icons/hicolor/scalable/apps/io.github.matrixshader.ShadowSettings.svg
  </files>
  <action>
  1. Rename the desktop file:
     - `data/com.github.matrixshader.construct.desktop` -> `data/io.github.matrixshader.ShadowSettings.desktop`
     - Update contents:
       - `Name=Shadow Settings`
       - `Comment=The settings hiding in the shadow of your system`
       - `Exec=shadow-settings`
       - `Icon=io.github.matrixshader.ShadowSettings`
       - Keep Categories, Keywords, Terminal, Type, StartupNotify as-is

  2. Rename the polkit policy file:
     - `polkit/com.github.matrixshader.construct.policy` -> `polkit/io.github.matrixshader.ShadowSettings.policy`
     - Update the action id: `io.github.matrixshader.ShadowSettings.write-system-config`
     - Update description: `Write system configuration (Shadow Settings)`
     - Keep defaults block as-is

  3. Rename the icon file:
     - `data/icons/hicolor/scalable/apps/com.github.matrixshader.construct.svg` -> `data/icons/hicolor/scalable/apps/io.github.matrixshader.ShadowSettings.svg`
     - No content changes needed (SVG content is not app-ID-dependent)

  4. Delete the old files after creating the new ones. Use `git mv` if git is initialized, otherwise manual rename.
  </action>
  <verify>
    <automated>test -f data/io.github.matrixshader.ShadowSettings.desktop && test -f polkit/io.github.matrixshader.ShadowSettings.policy && test -f data/icons/hicolor/scalable/apps/io.github.matrixshader.ShadowSettings.svg && ! test -f data/com.github.matrixshader.construct.desktop && ! test -f polkit/com.github.matrixshader.construct.policy && echo "PASS: all files renamed" || echo "FAIL: file rename incomplete"</automated>
  </verify>
  <done>Old files removed, new files exist with correct names and updated contents. No reference to "construct" or "com.github.matrixshader.construct" in any data/polkit file.</done>
</task>

<task type="auto">
  <name>Task 2: Update all source files and meson.build with new identity</name>
  <files>
    meson.build,
    src/main.vala,
    src/application.vala,
    src/window.vala,
    src/panels/power.vala,
    src/panels/windows.vala,
    src/panels/desktop.vala,
    src/panels/appearance.vala,
    src/panels/input.vala,
    src/helpers/logind-helper.vala
  </files>
  <action>
  1. Update `meson.build`:
     - Project name: `'shadow-settings'` (first arg to `project()`)
     - Executable name: `'shadow-settings'` (first arg to `executable()`)
     - Desktop file install_data path: `'data/io.github.matrixshader.ShadowSettings.desktop'`
     - Icon install_data path: `'data/icons/hicolor/scalable/apps/io.github.matrixshader.ShadowSettings.svg'`
     - Polkit install_data path: `'polkit/io.github.matrixshader.ShadowSettings.policy'`

  2. Update namespace in ALL Vala source files:
     - Replace `namespace Construct` with `namespace ShadowSettings` in every .vala file
     - Replace `Construct.` references with `ShadowSettings.` (e.g., `Construct.Application` -> `ShadowSettings.Application`, `Construct.Window` -> `ShadowSettings.Window`, etc.)
     - This affects: main.vala, application.vala, window.vala, all 5 panels, logind-helper.vala

  3. Update `src/application.vala`:
     - `application_id: "io.github.matrixshader.ShadowSettings"`

  4. Update `src/window.vala`:
     - Window title: `"Shadow Settings"` (was `"The Construct"`)
     - Sidebar header title: `"Shadow Settings"` (was `"The Construct"`)
     - Sidebar header subtitle: `"The Settings They Took"` (was `"Settings They Took"`)
     - Sidebar nav page title: `"Shadow Settings"` (was `"The Construct"`)

  5. Update `src/helpers/logind-helper.vala`:
     - Change `CONF_FILE` from `"/etc/systemd/logind.conf.d/99-construct.conf"` to `"/etc/systemd/logind.conf.d/99-shadow-settings.conf"`
     - This follows Pitfall 10 guidance: rename the drop-in file to match the new app name

  6. Verify: run `grep -rn "construct\|Construct\|com\.github\.matrixshader" src/ meson.build` and confirm zero hits.
  </action>
  <verify>
    <automated>cd /home/neo/shadow-settings && grep -rni "construct\|com\.github\.matrixshader" src/ meson.build data/ polkit/ 2>/dev/null | grep -v ".svg" | grep -v "Binary" ; test $? -eq 1 && echo "PASS: no old references found" || echo "FAIL: old references remain"</automated>
  </verify>
  <done>All source files use `ShadowSettings` namespace. Application ID is `io.github.matrixshader.ShadowSettings`. Binary name is `shadow-settings`. Logind drop-in uses `99-shadow-settings.conf`. Zero references to old "Construct" identity remain.</done>
</task>

<task type="auto">
  <name>Task 3: Build and verify the renamed app launches</name>
  <files>meson.build</files>
  <action>
  1. Clean any existing build directory: `rm -rf builddir`
  2. Configure: `meson setup builddir`
  3. Compile: `meson compile -C builddir`
  4. Verify binary exists: `ls -la builddir/shadow-settings`
  5. Verify binary size is reasonable (should be under 500KB)
  6. Verify the binary name is correct (not "construct")

  Do NOT attempt to run the GUI app (no display server in this context). The build succeeding with zero errors is the verification.

  If there are compile errors, fix them -- likely namespace mismatches or missed renames. Common issues:
  - Vala namespace `ShadowSettings` might conflict with class names -- ensure the namespace/class naming is clean
  - If `ShadowSettings.Application` causes issues, the class can stay as `Application` inside the `ShadowSettings` namespace
  </action>
  <verify>
    <automated>cd /home/neo/shadow-settings && rm -rf builddir && meson setup builddir 2>&1 && meson compile -C builddir 2>&1 && test -f builddir/shadow-settings && echo "PASS: build succeeded" || echo "FAIL: build failed"</automated>
  </verify>
  <done>App compiles cleanly with zero errors as `shadow-settings` binary. Binary exists at `builddir/shadow-settings`.</done>
</task>

</tasks>

<verification>
1. `grep -rni "construct\|com\.github\.matrixshader" src/ meson.build data/ polkit/` returns zero non-SVG matches
2. `meson setup builddir && meson compile -C builddir` succeeds with zero errors
3. `ls builddir/shadow-settings` shows the renamed binary exists
4. `grep "io.github.matrixshader.ShadowSettings" src/application.vala` confirms new app ID
5. `grep "shadow-settings" meson.build` confirms new binary name
</verification>

<success_criteria>
- App ID is `io.github.matrixshader.ShadowSettings` in application.vala
- Binary builds as `shadow-settings`
- Desktop file, polkit policy, and icon all use new app ID naming
- Logind drop-in config filename is `99-shadow-settings.conf`
- Zero references to "construct", "Construct", or "com.github.matrixshader.construct" remain in source
- Clean compile with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-identity-foundation/01-01-SUMMARY.md`
</output>
