---
phase: 05-search-polish-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - io.github.matrixshader.ShadowSettings.json
  - data/io.github.matrixshader.ShadowSettings.metainfo.xml
  - meson.build
  - README.md
autonomous: false
requirements:
  - NFR-2
  - NFR-3
  - NFR-4
  - NFR-5
user_setup:
  - service: github
    why: "Public repo needed for Flathub submission"
    env_vars: []
    dashboard_config:
      - task: "Create public GitHub repo matrixshader/shadow-settings"
        location: "github.com/new"
      - task: "Push code to main branch"
        location: "git remote add origin + git push"
  - service: flathub
    why: "App distribution"
    env_vars: []
    dashboard_config:
      - task: "Fork flathub/flathub repo"
        location: "github.com/flathub/flathub"
      - task: "Open submission PR manually (AI submission prohibited by Flathub policy)"
        location: "github.com/flathub/flathub/compare"

must_haves:
  truths:
    - "Flatpak manifest exists at repo root with correct app ID, runtime, and permissions"
    - "Metainfo XML exists with all Flathub-required fields (name, summary, description, developer, content_rating, releases, screenshots placeholder)"
    - "Metainfo is installed to correct path (share/metainfo/) via meson"
    - "Release build binary is under 500KB (NFR-3)"
    - "App compiles cleanly against GTK4 4.12+ and libadwaita 1.4+ (NFR-4)"
    - "README has install instructions for both native and Flatpak"
  artifacts:
    - path: "io.github.matrixshader.ShadowSettings.json"
      provides: "Flatpak manifest"
      contains: "org.gnome.Platform"
    - path: "data/io.github.matrixshader.ShadowSettings.metainfo.xml"
      provides: "Appstream metainfo"
      contains: "desktop-application"
    - path: "meson.build"
      provides: "Metainfo install rule"
      contains: "metainfo"
    - path: "README.md"
      provides: "Install instructions"
      contains: "shadow-settings"
  key_links:
    - from: "io.github.matrixshader.ShadowSettings.json"
      to: "meson.build"
      via: "Flatpak builder invokes meson with --buildtype=release"
      pattern: "buildtype.*release"
    - from: "data/io.github.matrixshader.ShadowSettings.metainfo.xml"
      to: "meson.build"
      via: "install_data for metainfo to share/metainfo/"
      pattern: "metainfo"
    - from: "io.github.matrixshader.ShadowSettings.json"
      to: "data/io.github.matrixshader.ShadowSettings.desktop"
      via: "command field matches binary name from desktop file"
      pattern: "shadow-settings"
---

<objective>
Create Flatpak manifest, Appstream metainfo, README, and verify all NFR targets for distribution readiness (NFR-2 performance, NFR-3 binary size, NFR-4 GNOME compat, NFR-5 Flatpak).

Purpose: The app is feature-complete after Plan 01. This plan packages it for distribution. Flathub requires a manifest, metainfo.xml, and screenshots. The human must open the Flathub PR manually (AI submission prohibited).

Output: Distribution-ready repo with Flatpak manifest, metainfo, README, and verified NFR compliance.
</objective>

<execution_context>
@/home/neo/.claude/get-shit-done/workflows/execute-plan.md
@/home/neo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search-polish-distribution/05-RESEARCH.md
@.planning/phases/05-search-polish-distribution/05-01-SUMMARY.md

@meson.build
@data/io.github.matrixshader.ShadowSettings.desktop

<interfaces>
<!-- From 05-RESEARCH.md — verified Flatpak/Flathub requirements -->

Flatpak manifest requirements:
- app-id: io.github.matrixshader.ShadowSettings
- runtime: org.gnome.Platform//48
- sdk: org.gnome.Sdk
- command: shadow-settings
- finish-args: --share=ipc, --socket=wayland, --socket=fallback-x11, --device=dri, --metadata=X-DConf=migrate-path
- config-opts: --buildtype=release (required for NFR-3 binary size)
- NO --filesystem=home, NO --socket=session-bus, NO --system-talk-name=org.freedesktop.login1

Metainfo requirements (Flathub):
- component type="desktop-application"
- developer id="io.github.matrixshader" with name child (NOT old developer_name tag)
- content_rating type="oars-1.1" (required, even if empty)
- releases with at least one release entry
- screenshots with at least one direct HTTP(S) image URL
- metadata_license: CC0-1.0
- project_license: GPL-3.0-or-later
- Install to: share/metainfo/ (NOT share/appdata/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flatpak manifest, metainfo, and meson install rules</name>
  <files>io.github.matrixshader.ShadowSettings.json, data/io.github.matrixshader.ShadowSettings.metainfo.xml, meson.build</files>
  <action>
**1. Create Flatpak manifest** at repo root: `io.github.matrixshader.ShadowSettings.json`

```json
{
    "app-id": "io.github.matrixshader.ShadowSettings",
    "runtime": "org.gnome.Platform",
    "runtime-version": "48",
    "sdk": "org.gnome.Sdk",
    "command": "shadow-settings",
    "finish-args": [
        "--share=ipc",
        "--socket=wayland",
        "--socket=fallback-x11",
        "--device=dri",
        "--metadata=X-DConf=migrate-path=/io/github/matrixshader/ShadowSettings/"
    ],
    "modules": [
        {
            "name": "shadow-settings",
            "builddir": true,
            "buildsystem": "meson",
            "config-opts": [
                "--buildtype=release"
            ],
            "sources": [
                {
                    "type": "git",
                    "url": "https://github.com/matrixshader/shadow-settings.git",
                    "branch": "main"
                }
            ]
        }
    ]
}
```

Key decisions:
- `--buildtype=release` ensures binary under 500KB (NFR-3)
- `--metadata=X-DConf=migrate-path` for GSettings migration (keyfile backend automatic with GNOME 48 runtime)
- NO `--filesystem=home`, NO `--socket=session-bus`, NO `--system-talk-name=org.freedesktop.login1` (logind auto-hidden via /.flatpak-info check)

**2. Create Appstream metainfo** at `data/io.github.matrixshader.ShadowSettings.metainfo.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.matrixshader.ShadowSettings</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>
  <name>Shadow Settings</name>
  <summary>The settings hiding in the shadow of your system</summary>
  <description>
    <p>
      Shadow Settings dynamically discovers and surfaces hidden GNOME
      desktop settings that are not exposed in the standard Settings app.
      It scans your system at runtime to find settings specific to your
      GNOME version and installed components.
    </p>
    <p>Features:</p>
    <ul>
      <li>Discovers hundreds of hidden GSettings keys automatically</li>
      <li>Organizes settings into logical categories</li>
      <li>Search across all discovered settings</li>
      <li>Reset any setting to its default value</li>
      <li>Three distinctive Art Deco visual themes</li>
    </ul>
  </description>
  <launchable type="desktop-id">io.github.matrixshader.ShadowSettings.desktop</launchable>
  <screenshots>
    <screenshot type="default">
      <caption>Shadow Settings showing the Desktop category</caption>
      <image>https://raw.githubusercontent.com/matrixshader/shadow-settings/main/data/screenshots/main.png</image>
    </screenshot>
  </screenshots>
  <url type="homepage">https://matrixshader.com</url>
  <url type="bugtracker">https://github.com/matrixshader/shadow-settings/issues</url>
  <url type="donation">https://buymeacoffee.com/iknowkungfu</url>
  <developer id="io.github.matrixshader">
    <name>Matrix Shader</name>
  </developer>
  <content_rating type="oars-1.1" />
  <releases>
    <release version="1.0.0" date="2026-03-13">
      <description>
        <p>Initial release.</p>
      </description>
    </release>
  </releases>
</component>
```

Note: The screenshot URL points to `data/screenshots/main.png` on GitHub. The human will take the screenshot and add it before the Flathub submission.

**3. Add metainfo install rule to meson.build:**

After the existing `install_data` for the gschema.xml, add:
```meson
install_data(
  'data/io.github.matrixshader.ShadowSettings.metainfo.xml',
  install_dir: get_option('datadir') / 'metainfo',
)
```

Use `share/metainfo/` path (not `share/appdata/` which is deprecated).
  </action>
  <verify>
    <automated>cd /home/neo/shadow-settings && meson compile -C builddir 2>&1 | tail -5</automated>
  </verify>
  <done>Flatpak manifest exists at repo root with correct app ID, GNOME Platform 48 runtime, release buildtype, and minimal sandbox permissions. Metainfo XML has all Flathub-required fields (developer with id attribute, content_rating, releases, screenshots placeholder, launchable). Meson installs metainfo to share/metainfo/. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create README and verify NFR compliance</name>
  <files>README.md</files>
  <action>
**1. Create README.md** at repo root with project description and install instructions:

The README should contain:
- Project name and tagline ("The settings hiding in the shadow of your system")
- Brief description of what the app does (dynamically discovers hidden GNOME settings)
- Features list (auto-discovery, categories, search, reset-to-default, themes, lid-close for native)
- Install section with Flatpak command (`flatpak install flathub io.github.matrixshader.ShadowSettings`)
- Build from source section with dependency lists for Fedora (`sudo dnf install vala meson gtk4-devel libadwaita-devel`) and Ubuntu/Debian (`sudo apt install valac meson libgtk-4-dev libadwaita-1-dev`)
- Build commands: `meson setup builddir && meson compile -C builddir && ./builddir/shadow-settings`
- System-wide install: `meson setup builddir --prefix=/usr && meson compile -C builddir && sudo meson install -C builddir`
- License: GPL-3.0-or-later
- Credits: Made by Matrix Shader with link to matrixshader.com

**2. Verify NFR-2 (Performance):**

Run a release build and check binary size:
```bash
meson setup builddir-rel --buildtype=release --wipe 2>/dev/null || meson setup builddir-rel --buildtype=release
meson compile -C builddir-rel
ls -la builddir-rel/shadow-settings
```

Confirm binary is under 500KB (expected: ~205KB unstripped, ~173KB stripped).

**3. Verify NFR-4 (GNOME Compat):**

Confirm the build uses only GTK4 4.12+ and libadwaita 1.4+ APIs by verifying `meson compile -C builddir` succeeds without version errors. All Gtk.SearchBar, Gtk.SearchEntry, Adw.StatusPage APIs are available since GTK4 4.0 / libadwaita 1.0.

Note: Do NOT run `flatpak-builder-lint` as the linter Flatpak may not be installed. The human can run linting before submission.
  </action>
  <verify>
    <automated>cd /home/neo/shadow-settings && meson compile -C builddir 2>&1 | tail -3 && test -f README.md && echo "README exists" && test -f io.github.matrixshader.ShadowSettings.json && echo "Manifest exists" && test -f data/io.github.matrixshader.ShadowSettings.metainfo.xml && echo "Metainfo exists"</automated>
  </verify>
  <done>README.md exists with native and Flatpak install instructions. Build succeeds (NFR-4). All distribution files present (manifest, metainfo, README). Binary size target achievable with release build (NFR-3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 5 and prepare for Flathub submission</name>
  <action>
Human verifies the complete Phase 5 output: search functionality, deprecated key filtering, distribution files, and NFR compliance. Then performs manual Flathub submission steps.

Build and launch: `meson setup builddir --wipe && meson compile -C builddir && ./builddir/shadow-settings`

Verification checklist:
1. Search button (magnifying glass) visible in sidebar header
2. Click search button or press Ctrl+F -- search bar appears
3. Type "font" -- results from multiple categories appear
4. Type nonsense "xyzxyz" -- "No Results" empty state shown
5. Press Escape -- search dismisses, previous panel returns
6. Search "menubar" or "tearoff" -- should return 0 results (deprecated keys filtered)
7. Sidebar subtitle shows result count during search, restores on dismiss
8. README.md content is accurate
9. Release binary under 500KB: `meson setup builddir-rel --buildtype=release && meson compile -C builddir-rel && ls -la builddir-rel/shadow-settings`

Before Flathub submission (human-only steps):
- Take at least one screenshot, save to `data/screenshots/main.png`
- Create GitHub repo `matrixshader/shadow-settings` and push to main
- Fork `flathub/flathub`, create branch, add manifest, open PR manually (AI submission prohibited by Flathub policy)
  </action>
  <verify>Human approval</verify>
  <done>Search works across all categories. Deprecated keys filtered. All distribution files present and correct. Human has verified the app and has clear instructions for Flathub submission.</done>
</task>

</tasks>

<verification>
1. `meson compile -C builddir` succeeds (NFR-4 compatibility)
2. Release binary under 500KB (NFR-3)
3. Search works across all categories (FR-6)
4. Deprecated keys filtered (user-requested feature)
5. All distribution files exist: manifest, metainfo, README
6. Metainfo installed to correct path via meson
</verification>

<success_criteria>
- Flatpak manifest with correct app ID, GNOME Platform 48, release buildtype, minimal permissions
- Metainfo with all Flathub-required fields installed to share/metainfo/
- README with native + Flatpak install instructions
- Release build binary under 500KB
- Build succeeds against GTK4 4.12+ / libadwaita 1.4+
- Human has clear instructions for screenshot capture and Flathub submission
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-polish-distribution/05-02-SUMMARY.md`
</output>
